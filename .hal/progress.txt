# Ralph Progress Log

## Codebase Patterns

- Factory bootstrap request/result DTOs live in `internal/factory`; add explicit JSON tags and lock exported machine-readable fields with raw-key plus round-trip tests in `internal/factory/types_test.go`.
- Bootstrap failure classification lives in `internal/factory/bootstrap_failure.go` with a separate bootstrap taxonomy (`repo`, `auth`, `dependency`, `engine_setup`, `unknown`) and sanitized messages that avoid raw command output.
- Factory bootstrap command execution goes through `internal/factory.BootstrapCommandExecutor` and `RunBootstrapStep`; tests should inject fake executors and deterministic clocks rather than spawning git, Hal, or engine CLIs directly.
- Repository checkout bootstrap goes through `internal/factory.BootstrapRepositoryCheckout`; inject `RepoExists`, executor, and clock dependencies so clone/fetch/checkout command construction is tested deterministically without touching real repositories.
- Run branch preparation stays in `BootstrapRepositoryCheckout` after base checkout; inject `LocalBranchExists`/`RemoteBranchExists` probes so tests cover local retry, remote resume, and first-run branch creation without real git refs.

---

## 2026-06-21 04:39 CST - US-001
- Implemented exported bootstrap request/result contracts in `internal/factory`, including options, step records, sanitized timeline events, and failure detail shape.
- Files changed: `internal/factory/bootstrap_types.go`, `internal/factory/types.go`, `internal/factory/types_test.go`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Factory bootstrap contracts belong in `internal/factory` alongside durable run/timeline DTOs, with command/runtime behavior added later behind injectable dependencies.
  - Machine-readable factory DTOs are guarded by explicit JSON tag reflection plus raw-key and round-trip tests in `internal/factory/types_test.go`.
  - `make lint` does not run static analysis when `golangci-lint` is missing; it prints the install hint and exits successfully.
---

## 2026-06-21 04:45 CST - US-002
- Implemented bootstrap failure classification constants and `ClassifyBootstrapFailure` for repo, auth, dependency, engine setup, and unknown outcomes with sanitized timeline-ready messages.
- Files changed: `internal/factory/bootstrap_failure.go`, `internal/factory/bootstrap_failure_test.go`, `internal/factory/types_test.go`, `internal/ci/auth.go`, `internal/ci/auth_test.go`, `internal/ci/push.go`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Bootstrap failure categories are intentionally separate from factory run pipeline categories because remote workspace setup needs repo/auth/dependency/engine setup distinctions.
  - Classification should prefer dependency and auth evidence before generic repository setup because git failures can carry executable-not-found or credential-specific signals.
  - Failure messages returned for bootstrap timeline/result output should be controlled summaries, not raw command output, so later environment and secret redaction stories have a safer base.
  - `make fmt` can surface existing gofmt alignment changes outside the story files; keep them if they are pure formatter output from the standard repo target.
---

## 2026-06-21 04:52 CST - US-003
- Implemented a bootstrap command execution boundary with `BootstrapCommand`, `BootstrapCommandResult`, `BootstrapCommandExecutor`, and `RunBootstrapStep` in `internal/factory`.
- Added fake-executor tests covering command ordering, arguments, working directories, environment values, deterministic timestamps, result metadata, missing executor handling, and classified command failures.
- Files changed: `internal/factory/bootstrap_executor.go`, `internal/factory/bootstrap_executor_test.go`, `internal/factory/types_test.go`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`, `.pi/skills/factory/SKILL.md`.
- **Learnings for future iterations:**
  - Keep bootstrap command construction as structured `BootstrapCommand` values so later repository, tooling, and Hal setup stories can assert command name, args, dir, and env without parsing shell strings.
  - `RunBootstrapStep` is the command-backed bootstrap helper to reuse for future orchestration; inject a clock in tests to keep step timestamps deterministic.
  - `hal repair --json` may normalize stale engine-local skill links before story work; check `.pi/skills/*` diffs deliberately before committing.
---

## 2026-06-21 05:06 CST - US-004
- Implemented repository checkout bootstrap with deterministic clone vs fetch command selection, base-branch checkout, dry-run planning, and repo/auth failure classification through the executor boundary.
- Files changed: `internal/factory/bootstrap_repository.go`, `internal/factory/bootstrap_repository_test.go`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Treat `BootstrapRequest.WorkspaceDir` as the final repository path for bootstrap repository setup; clone commands run from its parent directory and checkout/fetch commands run inside it.
  - Existing repository setup should fetch `origin` and then checkout the requested base branch; missing repository setup should clone first and then checkout the same base branch.
  - Dry-run repository bootstrap records pending `BootstrapStepResult` entries with command summaries instead of requiring an executor.
---

## 2026-06-21 05:13 CST - US-005
- Implemented idempotent run branch preparation in repository bootstrap, including local branch reuse, remote branch fetch/track checkout, and first-run branch creation from the requested base branch.
- Files changed: `internal/factory/bootstrap_repository.go`, `internal/factory/bootstrap_repository_test.go`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Run branch setup should happen after base checkout so branch creation always has a deterministic base ref.
  - Branch existence should be injected with `LocalBranchExists` and `RemoteBranchExists` in tests; the default path can probe via the executor boundary when a real executor exists.
  - Remote branch resume fetches the run branch into `refs/remotes/origin/<branch>` before `git checkout --track origin/<branch>` so checkout does not depend on implicit fetch refspec behavior.
---
