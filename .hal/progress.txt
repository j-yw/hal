# Ralph Progress Log

## Codebase Patterns

- Factory durable schema additions should preserve compatibility fields like `RunRecord.SandboxName` while adding richer optional objects; lock JSON field names and omission behavior in `internal/factory/types_test.go`.
- Factory machine contract docs are guarded by `cmd/contracts_doc_test.go`; add reflection-backed JSON tag checks there when documenting nested durable metadata objects so docs drift fails tests.
- Sandbox-backed factory execution side effects belong behind `factorySandboxExecutorDeps` in `cmd/factory_sandbox_executor.go`; add new remote behavior through normalized injectable dependencies so tests can fake registry, provisioning/start, provider exec, clocks, and store persistence.

---

## 2026-06-21 07:06 CST - US-001
- Added optional sandbox metadata to factory run records, including sandbox identity, provider, lifecycle status, safe connection display fields, SSH/cleanup commands, and handoff text.
- Preserved existing local factory run compatibility by keeping sandbox metadata optional and continuing to omit sandbox fields when absent.
- Added serialization tests for sandbox metadata field names, safe connection fields, forbidden secret-like fields, round-trip behavior, and optional omission.
- Files changed: `internal/factory/types.go`, `internal/factory/types_test.go`, `.hal/prd.json`, `.hal/progress.txt`.
- Quality checks: `go test ./internal/factory ./cmd`, `make test`, `make vet`.
- **Learnings for future iterations:**
  - Factory durable schemas use explicit JSON tags on exported fields, enforced by `TestFactoryTypesHaveJSONTags`.
  - Keep legacy summary fields such as `sandboxName` when adding richer metadata so existing records and consumers remain readable.
  - Safe sandbox metadata belongs in `internal/factory` contract types and should not include tokens, private keys, credentials, API keys, or raw env values.
---
## 2026-06-21 07:11 CST - US-002
- Documented sandbox metadata contract behavior for factory run/status machine-readable surfaces.
- Added `factory-status-v1` sandbox metadata and safe connection field tables, including redaction guidance and the `sandbox` executor mode.
- Updated the factory status contract example to show a sandbox-backed run record while keeping the compact factory run example free of full sandbox metadata.
- Added doc sync tests that assert sandbox metadata JSON tag fields from `internal/factory` are documented and that the examples match the intended schema split.
- Files changed: `docs/contracts/factory-run-v1.md`, `docs/contracts/factory-status-v1.md`, `docs/contracts/examples/factory-status-v1.json`, `cmd/contracts_doc_test.go`, `.hal/prd.json`, `.hal/progress.txt`, `AGENTS.md`.
- Quality checks: focused `go test ./cmd -run 'TestContractDocsIncludeFactoryFields|TestFactoryStatusDocsIncludeSandboxMetadataJSONFields|TestFactoryContractExamplesMatchCommandSchemas|TestMachineContractFields_FactoryCommandOutputs'`, `make test`, and `make vet` passed.
- **Learnings for future iterations:**
  - Factory `run --json` is intentionally compact; durable sandbox details belong in `factory status --json` via the run record.
  - Contract example tests can assert the intended split between compact result surfaces and durable status surfaces.
  - Use reflection over implementation JSON tags in docs tests when nested contract fields should stay synchronized with Go structs.
---
## 2026-06-21 07:17 CST - US-003
- Added the initial sandbox factory executor dependency layer with normalized production defaults for store access, clocks, sandbox resolution/loading, provisioning, start, provider resolution, provider exec, run-record saves, and timeline appends.
- Added the `sandbox` factory executor mode constant for sandbox-backed run records.
- Added focused tests proving nil sandbox executor dependencies default to production helpers and that fake dependencies cover explicit loading, default resolution, provisioning/start, provider execution, clock, and persistence boundaries without real sandbox providers or remote commands.
- Files changed: `cmd/factory_sandbox_executor.go`, `cmd/factory_sandbox_executor_test.go`, `internal/factory/types.go`, `.hal/prd.json`, `.hal/progress.txt`, `AGENTS.md`.
- Quality checks: `go test ./cmd ./internal/factory ./internal/sandbox`.
- **Learnings for future iterations:**
  - Keep sandbox-backed factory behavior in `cmd/factory_sandbox_executor.go` behind `factorySandboxExecutorDeps`; later stories should extend those injected boundaries instead of calling registry/provider/store functions directly.
  - Explicit sandbox targets should flow through the injectable loader, while no-target execution should use the injectable default resolver with the running-only filter.
  - The production provider exec adapter should run `Provider.Exec` commands through `sandbox.RunCmd` so stdout/stderr handling stays consistent with existing sandbox provider conventions.
---
