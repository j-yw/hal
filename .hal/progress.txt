## Codebase Patterns
- Postgres/Turso ClaimRun uses `FOR UPDATE SKIP LOCKED` subquery pattern for atomic claim — modifications go in the outer UPDATE SET clause.
- Integration tests behind `//go:build integration` tags need `go vet -tags=integration` to verify compilation since default `go test ./...` skips them.
- `TransitionRun(claimed -> queued)` is valid for re-queuing runs in tests to simulate retry scenarios.
- `scanRun` reads all RETURNING columns including `attempt_count`, so adding SET clauses to the UPDATE is automatically reflected in the returned Run struct.
- Turso store tests are regular unit tests (no build tags) using `modernc.org/sqlite`, while Postgres store tests require `//go:build integration` and `HAL_TEST_POSTGRES_DSN`.
- After US-001/US-002 made store adapters atomically increment `attempt_count`, ClaimService must use `run.AttemptCount` directly (not `+1`) for `Attempt.AttemptNumber` since the store already incremented it.
- Mock `claimedRun()` in claim_test.go represents the run as returned by `ClaimRun` (post-increment), so `AttemptCount` should reflect the incremented value (e.g., 1 for first claim, not 0).
- Use `cloud.ShellQuote(s)` from `internal/cloud/shell.go` for safe single-quote shell escaping — wraps in `'...'` with `'` → `'\''` replacement. The old `escapeShellSingleQuote` in auth_materialization.go only escapes inner content without wrapping.
- `CollectSandboxBundle` uses `MatchesArtifactPatterns` (workflow-aware filtering) not `IsBundlePathAllowed` (input allowlist) — these are two different filtering policies for worker-side vs client-side collection.
- When building shell commands with `ShellQuote`, wrap the entire path argument (e.g., `ShellQuote("/workspace/" + relPath)`) not just part of it.
- Use `mockCollectRunner` + `sandboxFS` pattern for testing sandbox exec operations without real infrastructure.
- `readSandboxFile` uses `stripBase64Whitespace` to strip all whitespace from base64 output before decoding — handles wrapped output from non-GNU base64 implementations and trailing newlines.
- `base64Cmd` is a package-level variable (`"base64 -w0"`) used by `readSandboxFile` — mock runners must use `base64Cmd` (not hardcoded `"base64 -w0"`) when matching command prefixes.
- Bundle framing uses `path\x00size\x00content` (null-terminated path, null-terminated decimal size, raw content bytes) plus gzip — `CompressBundle` (worker-side) and `compressBundleFiles` (client-side) produce identical framing consumed by `decompressBundleFiles`.
- Use `ComputeSandboxBundleHash(records)` for worker-side snapshot SHA — it converts `SandboxBundleRecord` → `BundleManifestRecord` via `NewBundleManifestRecord` then delegates to `ComputeBundleHash`. Never hash compressed payload bytes for the snapshot SHA field.
- `GitHubPRCreator(r, sandboxID, authDir)` returns a `PRCreator` closure — use `mockPRRunner` in tests to capture and assert the executed command text without real sandbox infrastructure.
- `parseGHPROutput` extracts the PR URL from `gh pr create` stdout by scanning the last non-empty line — `gh` may emit progress text before the URL.
- `WorkerPipeline` uses config-struct constructor pattern (`WorkerPipelineConfig` → `NewWorkerPipeline`) with explicit nil/empty checks for required dependencies (Store, Runner, WorkerID, Claim).
- `ProcessOne` maps `ErrNotFound` from `ClaimAndLock` to `ErrNoWork` — callers should use `IsNoWork(err)` to detect empty queue, not `IsNotFound`.
- Worker tests use `workerMockStore` embedding `mockStore` plus `workerMockRunner` implementing `runner.Runner` — reuse this pattern for future worker-related tests.
- `executeAttempt` setup order is deterministic: TransitionRun(claimed→running) → Provision → Bootstrap → AuthMaterialization → Preflight. Tests use a shared `callLog` to verify ordering.
- `AuthMaterializationService.Materialize` is a no-op when `AuthProfile.SecretRef` is nil — call-order tests must provide a profile with a non-nil `SecretRef` to observe auth materialization exec calls.
- Use `newTestWorkerPipeline(t, store, rnr)` helper to construct a fully-wired pipeline with all services for worker tests — avoids duplicating service creation boilerplate.
- `workerMockRunner` supports `execOverrides` map (command prefix → result/error) for per-command exec failure injection without affecting other commands.
- `handleSetupFailure(ctx, runID, attemptID, fromRunStatus, stage, cause)` transitions both attempt and run to failed — uses `fromRunStatus` to handle failures before and after the run reaches `running` status.
- `transitionAttemptCall` struct is defined in `claim_test.go` and shared across all test files in `package cloud` — don't redeclare it in new test files.

---

## 2026-02-13 - US-001
- Implemented atomic `attempt_count` increment in Postgres `ClaimRun`
- Files changed:
  - `internal/cloud/postgres/store.go` — Added `attempt_count = attempt_count + 1` to ClaimRun UPDATE
  - `internal/cloud/postgres/store_test.go` — Added `TestClaimIncrementsAttemptCount` and `TestClaimNoIncrementForIneligible`
- **Learnings for future iterations:**
  - The ClaimRun SQL pattern uses a subquery with `FOR UPDATE SKIP LOCKED` and the outer UPDATE applies all mutations atomically — extend the SET clause for any new claim-time state changes.
  - Postgres store tests are integration tests (`//go:build integration`) that require `HAL_TEST_POSTGRES_DSN` — use `go vet -tags=integration` to verify compilation without a live database.
  - The `validRunTransitions` map in `store.go` defines allowed state transitions — check it before writing tests that rely on `TransitionRun`.
---

## 2026-02-13 - US-002
- Implemented atomic `attempt_count` increment in Turso `ClaimRun`
- Files changed:
  - `internal/cloud/turso/store.go` — Added `attempt_count = attempt_count + 1` to ClaimRun UPDATE SET clause
  - `internal/cloud/turso/store_test.go` — Added `TestClaimIncrementsAttemptCount` and `TestClaimNoIncrementForIneligible`
- **Learnings for future iterations:**
  - Turso/SQLite ClaimRun uses the same subquery UPDATE pattern as Postgres but without `FOR UPDATE SKIP LOCKED` — SQLite's single-writer lock provides serialization instead.
  - Turso store tests are regular unit tests (no build tag) using `modernc.org/sqlite` — they run with `go test ./...` unlike Postgres tests which need `-tags=integration`.
  - The test pattern for claim attempt_count is identical across Postgres and Turso — reuse the same test structure when adapting claim-time mutations across adapters.
---

## 2026-02-13 - US-003
- Aligned ClaimService `Attempt.AttemptNumber` with claimed `Run.AttemptCount`
- Files changed:
  - `internal/cloud/claim.go` — Changed `AttemptNumber: run.AttemptCount + 1` to `AttemptNumber: run.AttemptCount`
  - `internal/cloud/claim_test.go` — Updated `claimedRun()` helper to return `AttemptCount: 1` (post-increment), renamed test case, added `TestClaimAttemptNumberAlignment` with first-claim and second-claim-after-requeue subtests
- **Learnings for future iterations:**
  - After US-001/US-002, the store adapters atomically increment `attempt_count` during `ClaimRun`, so the returned `Run.AttemptCount` already has the incremented value — ClaimService must not add `+1` again.
  - Mock stores in claim_test.go should return runs that simulate post-increment state (e.g., `AttemptCount: 1` for first claim) since the real store does the increment before returning.
  - The `Attempt.AttemptNumber == Run.AttemptCount` invariant should be asserted explicitly in tests as a regression guard.
---

## 2026-02-13 - US-004
- Added shared shell escaping helper `ShellQuote` in `internal/cloud/shell.go`
- Files changed:
  - `internal/cloud/shell.go` — New file with exported `ShellQuote(s string) string` function
  - `internal/cloud/shell_test.go` — New file with table-driven tests covering plain text, whitespace, embedded single quotes, multiple quotes, newlines, tabs, empty string, and special shell characters
- **Learnings for future iterations:**
  - `ShellQuote` wraps values in single quotes and escapes `'` → `'\''` — use this instead of the old `escapeShellSingleQuote` which only does inner escaping without wrapping.
  - US-005 will replace `escapeShellSingleQuote` usage in `auth_materialization.go` with `ShellQuote` — the caller will need to adjust the `printf` command format since `ShellQuote` includes the outer quotes.
  - `strings.ReplaceAll` is simpler and more idiomatic than a rune-by-rune loop for single-character replacement.
---

## 2026-02-13 - US-005
- Replaced ad-hoc `escapeShellSingleQuote` with shared `ShellQuote` in auth materialization command construction
- Files changed:
  - `internal/cloud/auth_materialization.go` — Switched `printf` format from `'%s'` with `escapeShellSingleQuote()` to `%s` with `ShellQuote()`, removed the now-unused `escapeShellSingleQuote` function
  - `internal/cloud/auth_materialization_test.go` — Replaced `TestEscapeShellSingleQuote` with `TestMaterializeUsesShellQuote` that verifies command strings contain `ShellQuote` output for various inputs (plain text, embedded quotes, newlines). Updated `secret_with_single_quotes_escaped` test to assert `ShellQuote` output directly.
- **Learnings for future iterations:**
  - When switching from an inner-only escaping function to one that wraps in quotes (`ShellQuote`), update the surrounding format string to remove the manual outer quotes — otherwise you get double-quoting.
  - Tests should assert the exact `ShellQuote(value)` output in command strings rather than testing for absence of unescaped characters — this directly validates the integration.
---

## 2026-02-13 - US-006
- Implemented `CollectSandboxBundle` in `internal/cloud/snapshot_collect.go` for workflow-aware sandbox artifact collection
- Files changed:
  - `internal/cloud/snapshot_collect.go` — New file with `CollectSandboxBundle(ctx, runner, sandboxID, workflowKind)` that lists files via `find` in sandbox, filters by `WorkflowDefaultArtifactPatterns` + `MatchesArtifactPatterns`, and reads content via base64-encoded exec calls. Also includes `SandboxBundleRecord` type and `readSandboxFile` helper.
  - `internal/cloud/snapshot_collect_test.go` — New file with 7 tests: run/auto/review workflow filtering, empty workspace, no matching files, list error, and workspace-relative path verification. Uses `mockCollectRunner` backed by virtual `sandboxFS`.
- **Learnings for future iterations:**
  - When constructing shell commands with `ShellQuote`, ensure the entire path (including any `/workspace/` prefix) is inside the quote — `fmt.Sprintf("base64 -w0 %s", ShellQuote(absPath))` not `fmt.Sprintf("base64 -w0 /workspace/%s", ShellQuote(relPath))`.
  - `WorkflowKindRun` produces only state artifacts; `WorkflowKindAuto` and `WorkflowKindReview` produce state + reports. This maps directly to `WorkflowArtifactGroups(kind)`.
  - The mock runner pattern (`mockCollectRunner` + `sandboxFS`) enables testing sandbox operations without real infrastructure — reuse this pattern for future sandbox-dependent tests.
  - `CollectSandboxBundle` uses `MatchesArtifactPatterns` (workflow-aware) not `IsBundlePathAllowed` (input allowlist) — these are two different filtering policies for different contexts (worker-side vs client-side).
---

## 2026-02-13 - US-007
- Implemented robust base64 handling in `readSandboxFile` for binary-safe and multiline content capture
- Files changed:
  - `internal/cloud/snapshot_collect.go` — Added `base64Cmd` variable, `stripBase64Whitespace` function, updated `readSandboxFile` to strip whitespace before decoding
  - `internal/cloud/snapshot_collect_test.go` — Added 5 new tests: `MultilineTextContent`, `BinarySafeContent`, `WrappedBase64Output`, `DecodeFailure`, `StripBase64Whitespace` (table-driven). Updated mock runner to use `base64Cmd` variable.
- **Learnings for future iterations:**
  - Different Linux containers may use GNU coreutils, BusyBox, or other base64 implementations — always strip whitespace from base64 output before decoding rather than relying solely on `-w0` flag.
  - The `base64Cmd` package variable allows mock runners to match command prefixes without hardcoding `"base64 -w0"` — update mock command matching when `base64Cmd` changes.
  - For wrapped base64 output tests, create a custom `mockCollectRunner` that inserts newlines every 76 chars rather than using the `sandboxFS`-backed default mock.
  - Decode failure errors must include both the file path and stderr output — this enables rapid operational debugging when sandbox file capture fails.
---

## 2026-02-13 - US-008
- Implemented `CompressBundle` in `internal/cloud/snapshot_collect.go` with `path\x00size\x00content` framing plus gzip compression
- Files changed:
  - `internal/cloud/snapshot_collect.go` — Added `CompressBundle(records []SandboxBundleRecord) ([]byte, error)` with gzip writer, added `bytes` and `compress/gzip` imports
  - `internal/cloud/snapshot_collect_test.go` — Added `decompressBundle` helper (mirrors `cmd/cloud.go` `decompressBundleFiles` exactly), plus 5 round-trip tests: `RoundTrip` (multi-file), `BinaryContent`, `EmptyRecords`, `EmptyFileContent`, `LargeContent`
- **Learnings for future iterations:**
  - The `decompressBundleFiles` function in `cmd/cloud.go` is unexported and in a different package — round-trip tests in `internal/cloud` must duplicate the decompression logic to verify framing compatibility.
  - The bundle framing format is `path\x00size\x00content` (null-terminated path, null-terminated decimal size, then raw content bytes) — this is the same format used by `compressBundleFiles` in `cmd/cloud.go` and must not change without updating both compress and decompress paths.
  - `CompressBundle` takes `[]SandboxBundleRecord` (worker-side) while `compressBundleFiles` takes `[]BundleManifestRecord` + `map[string][]byte` (client-side) — they produce the same framing but have different input interfaces.
  - Empty record slices produce valid gzip output that decompresses to zero records — no special-case handling needed.
---

## 2026-02-13 - US-009
- Implemented `ComputeSandboxBundleHash` and `SandboxRecordsToBundleManifest` in `internal/cloud/snapshot_collect.go` to bridge worker-side `SandboxBundleRecord` to the existing `ComputeBundleHash` pipeline
- Files changed:
  - `internal/cloud/snapshot_collect.go` — Added `SandboxRecordsToBundleManifest(records []SandboxBundleRecord) []BundleManifestRecord` and `ComputeSandboxBundleHash(records []SandboxBundleRecord) string`
  - `internal/cloud/snapshot_collect_test.go` — Added 8 tests: `TestSandboxRecordsToBundleManifest`, `TestComputeSandboxBundleHash_Deterministic`, `_OrderIndependent`, `_DifferentContentDifferentHash`, `_MatchesManifestHash`, `_NotCompressedPayloadHash`, `_EmptyRecords`, `_DoesNotMutateInput`
- **Learnings for future iterations:**
  - `ComputeSandboxBundleHash` is the canonical worker-side function for snapshot SHA computation — it converts `SandboxBundleRecord` → `BundleManifestRecord` (using `NewBundleManifestRecord` which normalizes paths and computes per-file SHA) then delegates to `ComputeBundleHash`.
  - Snapshot SHA must NEVER be a hash of the compressed payload bytes — it must be `ComputeBundleHash(records)` which hashes sorted `(normalized_path, file_sha256)` pairs. This ensures the same set of files always produces the same SHA regardless of compression.
  - `NewBundleManifestRecord` in `bundle_manifest.go` is the bridge from raw `(path, content)` to the `BundleManifestRecord` struct — reuse it instead of manually computing `ComputeFileSHA256` and `NormalizeBundlePath`.
  - The `_MatchesManifestHash` test is critical — it verifies that worker-side and submit-side hash computation produce identical results for equivalent file sets, ensuring cross-path SHA consistency.
---

## 2026-02-13 - US-010
- Implemented `GitHubPRCreator` in `internal/cloud/pr_github.go` returning a `PRCreator` function that executes `gh pr create` in the sandbox workspace
- Files changed:
  - `internal/cloud/pr_github.go` — New file with `GitHubPRCreator(r runner.Runner, sandboxID, authDir string) PRCreator`, `buildGHPRCreateCommand`, and `parseGHPROutput`
  - `internal/cloud/pr_github_test.go` — New file with 9 tests: Success, ExecutesGHPRCreate, NonZeroExit, RunnerError, TokenSourcing, ShellEscaping, EmptyBody, ParseGHPROutput (5 subtests), BuildGHPRCreateCommand
- **Learnings for future iterations:**
  - `GitHubPRCreator` is a factory function returning a `PRCreator` closure — it captures `runner.Runner`, `sandboxID`, and `authDir` so the returned function matches the `PRCreator` signature `func(ctx, *PRCreateRequest) (string, error)`.
  - The `gh pr create` command output has the PR URL as the last non-empty line of stdout — `parseGHPROutput` extracts it by scanning lines in reverse.
  - `buildGHPRCreateCommand` sources `GITHUB_TOKEN` from `${authDir}/credentials` using `export GITHUB_TOKEN=$(cat ...)` — the same auth directory pattern used by `auth_materialization.go`.
  - All user-controlled arguments (title, body, head, base, repo) are escaped with `ShellQuote` — use `mockPRRunner` pattern to capture and assert command text in tests.
  - US-011 and US-012 refine token sourcing details and error handling respectively — the core implementation already includes both for completeness since they're tightly coupled to the PR creator behavior.
---

## 2026-02-13 - US-011
- Added comprehensive tests verifying token sourcing and shell escaping in GitHubPRCreator
- Files changed:
  - `internal/cloud/pr_github_test.go` — Added `TestGitHubPRCreator_QuoteHeavyArguments` (verifies all 5 user-controlled args are ShellQuoted with complex quote patterns), added `TestGitHubPRCreator_TokenSourcingFromAuthDir` (verifies token export from authDir/credentials with special characters in path, and ordering before gh pr create)
- **Learnings for future iterations:**
  - The core implementation for US-011 was already complete from US-010 since token sourcing and escaping are tightly coupled to the PR creator. The story's value is in explicit test coverage for the acceptance criteria.
  - When verifying shell escaping, test all five user-controlled arguments (title, body, head, base, repo) in a single test case with quote-heavy values to ensure none are missed.
  - Token sourcing tests should verify both the exact `export GITHUB_TOKEN=$(cat ...)` pattern AND that it appears before `gh pr create` in the command string — ordering matters for shell evaluation.
  - Auth directories with special characters (e.g., apostrophes) in the path validate that `ShellQuote` is applied to the credentials file path, not just the user-controlled PR arguments.
---

## 2026-02-13 - US-012
- Added comprehensive tests for GitHubPRCreator execution failure surfacing with concrete error data
- Files changed:
  - `internal/cloud/pr_github_test.go` — Added `TestGitHubPRCreator_NonZeroExitIncludesExitCodeAndStderr` (table-driven: exit 1/128/2, with/without stderr), `TestGitHubPRCreator_RunnerErrorWrapping` (verifies `%w` wrapping via `errors.Is` plus operation context), `TestGitHubPRCreator_RunnerErrorVsNonZeroExit` (asserts distinct error text patterns for both branches)
- **Learnings for future iterations:**
  - The `pr_github.go` implementation already had correct error handling from US-010 — US-012's value is in explicit test coverage proving both error branches satisfy acceptance criteria.
  - Use `errors.Is(err, sentinel)` to verify `%w` wrapping rather than just substring matching — this confirms the error chain is properly maintained for callers using `errors.Is`/`errors.As`.
  - The two error branches produce distinct prefixes: `"executing gh pr create: <underlying>"` for runner errors and `"gh pr create failed (exit N): <stderr>"` for non-zero exits — tests should verify these are distinguishable so callers can differentiate failure modes.
---

## 2026-02-13 - US-013
- Implemented WorkerPipeline foundation with ErrNoWork sentinel and ProcessOne claim-to-nowork mapping
- Files changed:
  - `internal/cloud/worker.go` — New file with `ErrNoWork`, `IsNoWork`, `WorkerPipelineConfig`, `WorkerPipeline`, `NewWorkerPipeline`, and `ProcessOne`
  - `internal/cloud/worker_test.go` — New file with 6 tests: `TestNewWorkerPipeline` (5 subtests for valid config and each missing dependency), `TestProcessOne_NoWork`, `TestProcessOne_ClaimError`, `TestProcessOne_SuccessfulClaim`, `TestErrNoWork_Message`, `TestIsNoWork`
- **Learnings for future iterations:**
  - `WorkerPipelineConfig` uses the config-struct constructor pattern — all required dependencies (Store, Runner, WorkerID, Claim) are validated in `NewWorkerPipeline` with descriptive error messages.
  - `ProcessOne` maps `ErrNotFound` from `ClaimAndLock` to `ErrNoWork` so callers can distinguish "empty queue" (expected) from operational errors. Use `IsNoWork(err)` not `IsNotFound(err)` at the worker loop level.
  - The `workerMockRunner` must implement `runner.Runner` including `StreamLogs` returning `io.ReadCloser` — using an anonymous interface type causes a compile error even if structurally identical.
  - Future stories (US-014+) will extend `ProcessOne` with setup, execution, finalization, and cleanup stages — the `_ = result` placeholder marks where that code will go.
---

## 2026-02-13 - US-014
- Implemented deterministic `executeAttempt` setup ordering in `WorkerPipeline`
- Files changed:
  - `internal/cloud/worker.go` — Added `Provision`, `Bootstrap`, `AuthMaterialization`, `Preflight` to `WorkerPipelineConfig` and `WorkerPipeline`; added validation in `NewWorkerPipeline`; implemented `executeAttempt` with deterministic setup order: TransitionRun(claimed→running) → Provision → Bootstrap → AuthMaterialization → Preflight; wired `executeAttempt` into `ProcessOne`
  - `internal/cloud/worker_test.go` — Added `callLog` for ordering assertions, `runTransition` tracking, `execOverrides` map for per-command failure injection, `newTestWorkerPipeline` helper, `testClaimedRun` helper; added 8 new tests: `TestExecuteAttempt_TransitionsClaimedToRunningBeforeSetup`, `TestExecuteAttempt_SetupCallOrder`, `TestExecuteAttempt_TransitionRunFailure`, `TestExecuteAttempt_ProvisionFailure`, `TestExecuteAttempt_BootstrapFailure`, `TestExecuteAttempt_AuthMaterializationFailure`, `TestExecuteAttempt_PreflightFailure`, `TestExecuteAttempt_SetupCallOrderExact`; updated all existing tests to provide new required service dependencies
- **Learnings for future iterations:**
  - `executeAttempt` transitions run to `running` before any setup work so that setup failures can be attributed to the `running` status (US-015 will add status-aware failure handling).
  - `AuthMaterializationService.Materialize` silently no-ops when `SecretRef` is nil — tests that need to observe auth materialization behavior must provide a profile with a non-nil `SecretRef`.
  - Use a shared `callLog` struct across mock store and runner to verify cross-service call ordering. Each mock records a descriptive entry (e.g., `"provision"`, `"bootstrap"`) when its key method is invoked.
  - `workerMockRunner.execOverrides` allows targeting specific commands for failure without affecting the default success path — key for testing individual setup stage failures in isolation.
  - The `Preflight` service with no `ProviderCommands` configured only calls `GetAuthProfile` and emits events, without issuing any `Exec` calls — to test preflight failure, either set incompatible `RuntimeMetadataJSON` or configure a `ProviderCommands` entry with a failing command override.
---

## 2026-02-13 - US-015
- Implemented status-aware setup failure transitions in `WorkerPipeline.executeAttempt` with `handleSetupFailure` helper
- Files changed:
  - `internal/cloud/worker.go` — Added `handleSetupFailure(ctx, runID, attemptID, fromRunStatus, stage, cause)` method that transitions both attempt and run to failed using the correct source status; added `currentStatus` tracking in `executeAttempt` that starts at `claimed` and advances to `running` after successful transition; all setup failure paths now call `handleSetupFailure` before returning errors; added `time` import
  - `internal/cloud/worker_test.go` — Added `TransitionAttempt` tracking to `workerMockStore` with `transitionAttemptCalls` slice; added 5 new tests: `TestHandleSetupFailure_ProvisionFailsFromRunning` (verifies running→failed after successful transition), `TestHandleSetupFailure_TransitionToRunningFailsFromClaimed` (verifies claimed→failed when transition never completed), `TestHandleSetupFailure_BootstrapFailsFromRunning`, `TestHandleSetupFailure_SetupFailureCallOrder` (verifies attempt transition before run transition), `TestHandleSetupFailure_NoSetupCallsAfterFailure`; updated `TestExecuteAttempt_ProvisionFailure` to expect 2 transitions instead of 1
- **Learnings for future iterations:**
  - `handleSetupFailure` accepts `fromRunStatus` so it transitions from the correct source status — `claimed` if the transition to `running` never completed, `running` if it did. This prevents invalid transitions.
  - `handleSetupFailure` transitions attempt before run (attempt→failed, then run→failed) — this ordering ensures attempt state is recorded before run state changes.
  - The `transitionAttemptCall` struct is shared across `claim_test.go` and `worker_test.go` in the same package — don't redeclare it in new test files.
  - Setup failure error codes use `"setup_failure"` and error messages include the failing stage name — this enables operational debugging to identify which setup step caused the failure.
  - When adding failure handling to `executeAttempt`, existing tests that assert on transition counts need updating since each failure path now generates additional transitions.
---
