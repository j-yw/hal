# Ralph Progress Log

## Codebase Patterns

- ADRs live under `docs/adr/` using numbered lowercase slug filenames (for example `0001-topic.md`) and should include explicit `Status`, related issue, and scope sections.
- Machine-readable CLI compatibility should reference `docs/contracts/` as the canonical source; ADRs should separate stable JSON contract fields from hidden backend implementation details.
- Future control-plane ADR sections should distinguish organization-level ownership from project-level membership, and should not present future domain terms as CLI JSON contract fields unless `docs/contracts/` is updated.
- Future RBAC/control-plane ADR sections should name authorization as a shared control-plane responsibility while keeping permission evaluation internals out of CLI JSON contracts unless `docs/contracts/` is updated.
- Future shared queue/run lifecycle ADR sections should document leases, heartbeats, cancellation, stale recovery, concurrency, and idempotency as architecture expectations without exposing backend attempt IDs, idempotency keys, or lease internals as CLI JSON contract fields unless `docs/contracts/` is updated.
- Future shared artifact/audit ADR sections should cover PRDs, reports, logs, review results, run output, audit decisions, state transitions, and data ownership/access expectations without exposing storage keys, backend IDs, role graphs, or permission evaluation internals as CLI JSON contract fields unless `docs/contracts/` is updated.
- Future policy inheritance ADR sections should define precedence from built-in defaults through organization, project, run, and local overrides while keeping policy evaluation internals out of CLI JSON contracts unless `docs/contracts/` is updated.
- Future migration strategy ADR sections should phase local `.hal/` state toward shared control-plane authority through shadowing, artifact mirroring, opt-in shared queues, and opt-in shared run coordination; preserve current local CLI behavior early and list compatibility gates/open questions before introducing hosted or networked behavior.

---

## 2026-06-20 23:23 CST - US-001
- Created the ADR shell for Hal's future team and organization factory control plane.
- Files changed: `docs/adr/0001-hal-future-team-organization-factory-control-plane.md`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - The repository did not previously have a `docs/adr/` convention, so this story establishes numbered ADR filenames under that directory.
  - For documentation-only architecture work, `make test` provides Go typecheck coverage and `make vet` is the companion static check.
  - Keep ADR scope explicit when no runtime behavior or machine-readable CLI contracts are intended to change.
---

## 2026-06-20 23:27 CST - US-002
- Documented Hal's current local execution boundary in the factory control plane ADR.
- Files changed: `docs/adr/0001-hal-future-team-organization-factory-control-plane.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Current Hal workflow state should be described as local to the active worktree's `.hal/` directory until a future PRD explicitly introduces a shared state source.
  - Documentation-only ADR stories still use `make test` and `make vet` as the quality gate for typecheck/static coverage.
  - `golangci-lint` was not installed in this environment, so `make lint` was skipped rather than relying on the Makefile's non-failing install hint.
---

## 2026-06-20 23:30 CST - US-003
- Documented the CLI machine contract boundary in the factory control plane ADR, covering `hal status --json`, `hal doctor --json`, `hal continue --json`, `hal auto --json`, review-loop artifacts, report artifacts, stable contract areas, and additive extension rules.
- Files changed: `docs/adr/0001-hal-future-team-organization-factory-control-plane.md`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Formal JSON CLI contract behavior is documented under `docs/contracts/`; ADRs should reference those docs rather than restating implementation internals as public API.
  - Review-loop JSON artifacts are persisted under `.hal/reports/` and use the shared `compound.ReviewLoopResult` shape for automation-facing review summaries.
  - Documentation-only ADR stories should still run `make test` and `make vet`; `make lint` may be skipped when `golangci-lint` is unavailable.
---

## 2026-06-20 23:34 CST - US-004
- Added the control-plane domain model section to the factory control plane ADR, defining users, owners, organizations, projects, project membership, and the distinction between organization ownership and project membership.
- Files changed: `docs/adr/0001-hal-future-team-organization-factory-control-plane.md`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Keep future control-plane domain concepts in the ADR as architecture terminology, not as new CLI JSON contract fields unless a future contract doc explicitly adds them.
  - Organization ownership should be described separately from project membership so later RBAC, policy, and artifact sections can use consistent authorization boundaries.
  - Documentation-only ADR stories continue to use `make test` and `make vet`; `golangci-lint` was unavailable in this environment.
---

## 2026-06-20 23:37 CST - US-005
- Added the RBAC and authorization boundary section to the factory control plane ADR, covering queue operations, run operations, artifact access, policy operations, and the control-plane responsibility for authorization decisions.
- Files changed: `docs/adr/0001-hal-future-team-organization-factory-control-plane.md`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Future RBAC sections should describe permission expectations by operation class while avoiding specific role graphs unless a future implementation PRD defines them.
  - Authorization internals should remain hidden backend details and should not appear in CLI JSON contracts unless a future `docs/contracts/` revision explicitly adds them.
  - Documentation-only ADR stories continue to use `make test` and `make vet`; `golangci-lint` was unavailable in this environment, so `make lint` was skipped.
---

## 2026-06-20 23:40 CST - US-006
- Added the shared queue and run lifecycle section to the factory control plane ADR, covering queue item lifecycle, run leases, heartbeat requirements, cancellation, stale lease recovery, concurrency, and idempotency expectations.
- Files changed: `docs/adr/0001-hal-future-team-organization-factory-control-plane.md`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Shared lifecycle concepts should remain architecture expectations in the ADR until a future implementation PRD and `docs/contracts/` update make them runtime or CLI contract fields.
  - Lease, heartbeat, and idempotency details are useful for future backend design, but should be described without binding the current CLI to backend attempt IDs or idempotency keys.
  - Documentation-only ADR stories continue to use `make test` and `make vet`; `make lint` may be skipped when `golangci-lint` is unavailable.
---

## 2026-06-20 23:43 CST - US-007
- Added the shared artifact and audit model section to the factory control plane ADR, covering shared PRD, report, log, review-result, and run-output artifacts plus audit logging for decisions and state transitions.
- Files changed: `docs/adr/0001-hal-future-team-organization-factory-control-plane.md`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Shared artifact sections should describe ownership, access, retention, and audit expectations without turning storage keys or backend identifiers into CLI contract fields.
  - Artifact/audit documentation should keep local `.hal/` files as the current CLI runtime boundary until future implementation PRDs and contract updates define shared behavior.
  - Documentation-only ADR stories continue to use `make test` and `make vet`; `make lint` may be skipped when `golangci-lint` is unavailable.
---

## 2026-06-20 23:47 CST - US-008
- Added the policy inheritance and overrides section to the factory control plane ADR, covering organization defaults and guardrails, project refinements, run-level overrides, local developer overrides, and deterministic precedence.
- Files changed: `docs/adr/0001-hal-future-team-organization-factory-control-plane.md`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Policy inheritance should be described as control-plane architecture and not as new CLI JSON contract fields unless a future `docs/contracts/` update adds them.
  - Local overrides are appropriate for developer ergonomics, but ADR wording should keep shared authorization, artifact access, retention, lease ownership, and required gates under control-plane policy.
  - Documentation-only ADR stories continue to use `make test` and `make vet`; `make lint` may be skipped when `golangci-lint` is unavailable.
---

## 2026-06-20 23:52 CST - US-009
- Added the migration strategy section to the factory control plane ADR, covering local file-state migration, local queue coordination migration, preservation of existing CLI behavior, a phased plan, compatibility gates, and open questions for future implementation PRDs.
- Files changed: `docs/adr/0001-hal-future-team-organization-factory-control-plane.md`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Migration strategy sections should keep local `.hal/` files authoritative for current CLI behavior until a future implementation PRD and contract update deliberately move responsibility to shared state.
  - Shared queue migration should be documented separately from artifact mirroring because queue authority depends on leases, heartbeats, stale recovery, RBAC, audit, and idempotency being implemented and tested.
  - Documentation-only ADR stories continue to use `make test` and `make vet`; `make lint` was skipped because `golangci-lint` was unavailable.
---
