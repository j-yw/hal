# Ralph Progress Log

## Codebase Patterns

- Factory queue JSON contracts are split between reusable durable types in `internal/factory` (`QueueEntry`, `QueueClaim`, `QueueStatus*`) and command response DTOs in `cmd/factory_queue_contracts.go`; keep docs/examples/tests aligned when implementing queue commands.
- Factory queue storage builds on `internal/factory.Store`: keep durable queue state at the global config-backed factory root (`StoreDir()/queue.json`), return empty state for missing files without creating directories, and preserve corrupt files by returning parse errors without mutation.
- Queue read-modify-write paths should use `Store.UpdateQueue`; it holds `queue.lock` across load, mutation, and temp-file-plus-rename save so concurrent local queue workers preserve all updates.

---
## 2026-06-21 04:50:06 CST - US-001
- Implemented factory queue JSON contract definitions for queue entries and add/list/work command response DTOs.
- Added docs for `factory-queue-entry-v1`, `factory-queue-add-v1`, `factory-queue-list-v1`, and `factory-queue-work-v1`, plus add/list/work-claimed/work-noop example payloads.
- Added contract tests for queue entry JSON keys, queue response JSON keys, and strict docs/example schema sync.
- Files changed: `internal/factory/types.go`, `internal/factory/types_test.go`, `cmd/factory_queue_contracts.go`, `cmd/machine_contracts_test.go`, `cmd/contracts_doc_test.go`, `docs/contracts/factory-queue-*.md`, `docs/contracts/examples/factory-queue-*.json`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Factory machine contracts are locked in three places: DTO JSON key tests, contract docs, and strict example decoding with `DisallowUnknownFields`.
  - Queue command implementations should reuse `factory.QueueEntry` and the `FactoryQueue*Response` structs instead of defining separate response shapes.
  - Focused checks run so far: `go test -timeout=120s ./internal/factory ./cmd`.
---
## 2026-06-21 04:56:42 CST - US-002
- Implemented global config-backed factory queue storage in `internal/factory/queue.go` with `QueuePath`, `LoadQueue`, and `SaveQueue`.
- Added deterministic queue storage tests for global path placement, missing-file empty state, durable save/load behavior, file permissions, temp cleanup, and corrupt JSON preservation.
- Files changed: `internal/factory/queue.go`, `internal/factory/queue_test.go`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Reuse `factory.Store` for queue persistence so future queue commands share the same global config precedence as existing factory run/timeline storage.
  - Read-only queue loads should not create global directories; command implementations can treat a missing queue file as an empty queue.
  - Browser verification skipped because this story only changes backend queue storage.
  - Checks run: `go test ./internal/factory`, `make test`, `make vet`.
---
## 2026-06-21 05:04:02 CST - US-003
- Implemented atomic queue mutation support with `Store.UpdateQueue`, a local `queue.lock` guard, and locked `SaveQueue` writes.
- Added deterministic queue tests for failed save preservation, temp cleanup, and concurrent read-modify-write mutation safety.
- Files changed: `internal/factory/queue.go`, `internal/factory/queue_test.go`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`.
- **Learnings for future iterations:**
  - Use `Store.UpdateQueue` for enqueue/claim/lifecycle changes so load, mutation, and save happen under one local lock.
  - `SaveQueue` is still useful for whole-state replacement, but command-level mutations should avoid separate `LoadQueue`/`SaveQueue` sequences.
  - Browser verification skipped because this story only changes backend queue storage.
  - Checks run: `go test ./internal/factory`, `make test`, `make vet`.
---
