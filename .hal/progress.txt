# Ralph Progress Log

## Codebase Patterns

- Factory durable schema additions should preserve compatibility fields like `RunRecord.SandboxName` while adding richer optional objects; lock JSON field names and omission behavior in `internal/factory/types_test.go`.
- Factory machine contract docs are guarded by `cmd/contracts_doc_test.go`; add reflection-backed JSON tag checks there when documenting nested durable metadata objects so docs drift fails tests.
- Sandbox-backed factory execution side effects belong behind `factorySandboxExecutorDeps` in `cmd/factory_sandbox_executor.go`; add new remote behavior through normalized injectable dependencies so tests can fake registry, provisioning/start, provider exec, clocks, and store persistence.
- `hal factory run --sandbox` selection lives in `factoryRunRequest.Sandbox` and dispatches through `factoryRunDeps.runSandbox`; keep local execution behind `runPipeline` so no-flag factory runs stay on the existing local path.
- Existing sandbox reuse in `runFactorySandboxExecutorWithDeps` should persist `RunRecord.SandboxName` and `RunRecord.Sandbox` immediately after target resolution/start; no-name resolution should keep `sandbox.ResolveDefault` running-only error strings unless a later provisioning path intentionally handles them.
- Sandbox provision/start failures should be persisted inside `runFactorySandboxExecutorWithDeps` with `RunStatusFailed`, `FailureCategoryPipeline`, and sandbox handoff metadata before returning the wrapped error to the outer factory runner.
- Sandbox remote execution should carry a structured `factoryRunAutoRequest` from factory dispatch into `factorySandboxExecutorRequest`; build exact provider args with `factorySandboxRemoteAutoArgs` at the sandbox executor boundary and avoid adding env/config pass-through unless explicitly required.
- Sandbox remote output timeline capture belongs at the sandbox executor boundary: tee provider exec output to the user-facing writer, split complete lines, sanitize with the sandbox redactor, and persist `command_output_summary` events tagged with `source=remote_sandbox`.
- Sandbox executor remote lifecycle events should be appended through `factorySandboxTimelineWriter` so start/output/completion events share one sequence counter and consistent `source=remote_sandbox` metadata before the outer factory runner records terminal success/failure.

---

## 2026-06-21 07:06 CST - US-001
- Added optional sandbox metadata to factory run records, including sandbox identity, provider, lifecycle status, safe connection display fields, SSH/cleanup commands, and handoff text.
- Preserved existing local factory run compatibility by keeping sandbox metadata optional and continuing to omit sandbox fields when absent.
- Added serialization tests for sandbox metadata field names, safe connection fields, forbidden secret-like fields, round-trip behavior, and optional omission.
- Files changed: `internal/factory/types.go`, `internal/factory/types_test.go`, `.hal/prd.json`, `.hal/progress.txt`.
- Quality checks: `go test ./internal/factory ./cmd`, `make test`, `make vet`.
- **Learnings for future iterations:**
  - Factory durable schemas use explicit JSON tags on exported fields, enforced by `TestFactoryTypesHaveJSONTags`.
  - Keep legacy summary fields such as `sandboxName` when adding richer metadata so existing records and consumers remain readable.
  - Safe sandbox metadata belongs in `internal/factory` contract types and should not include tokens, private keys, credentials, API keys, or raw env values.
---
## 2026-06-21 07:11 CST - US-002
- Documented sandbox metadata contract behavior for factory run/status machine-readable surfaces.
- Added `factory-status-v1` sandbox metadata and safe connection field tables, including redaction guidance and the `sandbox` executor mode.
- Updated the factory status contract example to show a sandbox-backed run record while keeping the compact factory run example free of full sandbox metadata.
- Added doc sync tests that assert sandbox metadata JSON tag fields from `internal/factory` are documented and that the examples match the intended schema split.
- Files changed: `docs/contracts/factory-run-v1.md`, `docs/contracts/factory-status-v1.md`, `docs/contracts/examples/factory-status-v1.json`, `cmd/contracts_doc_test.go`, `.hal/prd.json`, `.hal/progress.txt`, `AGENTS.md`.
- Quality checks: focused `go test ./cmd -run 'TestContractDocsIncludeFactoryFields|TestFactoryStatusDocsIncludeSandboxMetadataJSONFields|TestFactoryContractExamplesMatchCommandSchemas|TestMachineContractFields_FactoryCommandOutputs'`, `make test`, and `make vet` passed.
- **Learnings for future iterations:**
  - Factory `run --json` is intentionally compact; durable sandbox details belong in `factory status --json` via the run record.
  - Contract example tests can assert the intended split between compact result surfaces and durable status surfaces.
  - Use reflection over implementation JSON tags in docs tests when nested contract fields should stay synchronized with Go structs.
---
## 2026-06-21 07:17 CST - US-003
- Added the initial sandbox factory executor dependency layer with normalized production defaults for store access, clocks, sandbox resolution/loading, provisioning, start, provider resolution, provider exec, run-record saves, and timeline appends.
- Added the `sandbox` factory executor mode constant for sandbox-backed run records.
- Added focused tests proving nil sandbox executor dependencies default to production helpers and that fake dependencies cover explicit loading, default resolution, provisioning/start, provider execution, clock, and persistence boundaries without real sandbox providers or remote commands.
- Files changed: `cmd/factory_sandbox_executor.go`, `cmd/factory_sandbox_executor_test.go`, `internal/factory/types.go`, `.hal/prd.json`, `.hal/progress.txt`, `AGENTS.md`.
- Quality checks: `go test ./cmd ./internal/factory ./internal/sandbox`.
- **Learnings for future iterations:**
  - Keep sandbox-backed factory behavior in `cmd/factory_sandbox_executor.go` behind `factorySandboxExecutorDeps`; later stories should extend those injected boundaries instead of calling registry/provider/store functions directly.
  - Explicit sandbox targets should flow through the injectable loader, while no-target execution should use the injectable default resolver with the running-only filter.
  - The production provider exec adapter should run `Provider.Exec` commands through `sandbox.RunCmd` so stdout/stderr handling stays consistent with existing sandbox provider conventions.
---
## 2026-06-21 07:23 CST - US-004
- Added the `--sandbox` flag to `hal factory run` command metadata, help text, and examples.
- Wired sandbox selection into factory run parsing and dispatch while preserving the existing no-flag local pipeline path.
- Added tests for sandbox flag registration, sandbox request parsing, local default execution, and sandbox executor selection with deterministic remote auto args.
- Files changed: `cmd/factory.go`, `cmd/factory_test.go`, `.hal/prd.json`, `.hal/progress.txt`, `AGENTS.md`.
- Quality checks: focused `go test ./cmd -run 'TestFactoryCommandHelpMetadata|TestParseFactoryRunRequest|TestFactoryRunCommandRegisteredWithInputFlags|TestRunFactoryRunWithDepsDefaultsToLocalPipelineWithoutSandboxFlag|TestRunFactoryRunWithDepsSelectsSandboxExecutorWithSandboxFlag'`, `make test`, `make vet`.
- **Learnings for future iterations:**
  - Factory run mode selection happens before pipeline execution by setting `ExecutorMode` from `factoryRunRequest.Sandbox`.
  - Add new factory run execution modes as injected `factoryRunDeps` functions so tests can assert dispatch without invoking real CLIs, providers, or network calls.
  - Generated CLI docs are intentionally left to the dedicated documentation story when command metadata changes.
---
## 2026-06-21 07:26 CST - US-005
- Reused existing managed sandboxes for sandbox-backed factory execution by resolving explicit targets through the injected loader and no-name targets through the injected `sandbox.ResolveDefault` running-only path.
- Persisted resolved sandbox name, provider, status, safe connection display fields, and handoff commands on the local factory run record before remote provider execution.
- Preserved default-resolution error strings for no-name sandbox runs instead of provisioning on empty or ambiguous registry state.
- Added fake-dependency tests for explicit and default sandbox resolution, metadata persistence, running-only filtering, provider exec inputs, and default-resolution errors without real providers or network calls.
- Files changed: `cmd/factory_sandbox_executor.go`, `cmd/factory_sandbox_executor_test.go`, `.hal/prd.json`, `.hal/progress.txt`, `AGENTS.md`.
- Quality checks: focused `go test ./cmd -run 'TestRunFactorySandboxExecutorWithDeps|TestNormalizeFactorySandboxExecutorDeps'`, `make test`, and `make vet` passed.
- **Learnings for future iterations:**
  - Persist sandbox metadata as soon as a target is selected so later execution/failure paths can rely on durable local handoff context.
  - No-name sandbox factory runs should use the same running-only filter semantics as `sandbox.ResolveDefault`; tests should assert stopped sandboxes are rejected by that filter.
  - Keep provisioning behavior explicit by target mode: explicit missing targets can still flow through the provision dependency, while default registry errors remain visible until a provisioning story intentionally changes that behavior.
---
## 2026-06-21 07:31 CST - US-006
- Added sandbox provisioning when default running-only resolution reports no usable sandbox, deriving default sandbox names from the factory run branch while preserving ambiguous default-resolution errors.
- Kept explicit missing sandbox targets on the provision path and stopped sandbox targets on the injected start path, with persisted sandbox metadata before remote execution.
- Recorded provision/start failures as failed factory run records with pipeline failure summaries, failed step names, and safe sandbox handoff metadata when a sandbox name or state is known.
- Added fake-dependency tests for no-usable-target provisioning, ambiguous default-resolution preservation, provision failure persistence, and start failure handoff metadata.
- Files changed: `cmd/factory_sandbox_executor.go`, `cmd/factory_sandbox_executor_test.go`, `.hal/prd.json`, `.hal/progress.txt`, `AGENTS.md`.
- Quality checks: focused `go test ./cmd -run 'TestRunFactorySandboxExecutorWithDeps|TestNormalizeFactorySandboxExecutorDeps'`, `make test`, and `make vet` passed.
- **Learnings for future iterations:**
  - Treat only exact `sandbox.ResolveDefault` no-match strings (`no sandboxes found`, `no running sandboxes`) as provisionable; ambiguity should remain a user-visible resolver error.
  - For sandbox lifecycle failures, save the run failure and handoff metadata in the sandbox executor before returning so outer factory result handling can preserve context.
  - Default sandbox names for factory provisioning should be derived from the factory branch, while repository cloning/setup still uses the run record repo remote.
---
## 2026-06-21 07:36 CST - US-007
- Routed sandbox-backed factory execution through a structured remote auto request and made the sandbox executor build deterministic `hal auto` provider args at execution time.
- Kept remote execution on the existing sandbox provider `Exec` path with `ConnectInfoFromState`, without introducing any environment or config value pass-through.
- Added tests for exact remote command construction across auto-discovery, markdown, report, base-branch, and empty-argument cases, plus updated dispatch assertions for the structured request.
- Files changed: `cmd/factory.go`, `cmd/factory_sandbox_executor.go`, `cmd/factory_test.go`, `cmd/factory_sandbox_executor_test.go`, `.hal/prd.json`, `.hal/progress.txt`, `AGENTS.md`.
- Quality checks: focused `go test ./cmd -run 'TestRunFactoryRunWithDepsSelectsSandboxExecutorWithSandboxFlag|TestRunFactorySandboxExecutorWithDeps|TestFactorySandboxRemoteAutoArgs|TestRunFactoryRunPipelineWithDeps'`, `go test ./cmd ./internal/factory ./internal/sandbox`, `make test`, and `make vet` passed.
- **Learnings for future iterations:**
  - Use `factoryRunAutoRequestFromFactoryRequest` to keep local and sandbox auto request semantics aligned.
  - Build provider command args in `factorySandboxRemoteAutoArgs` so tests can assert the exact remote `hal auto` invocation.
  - Do not pass arbitrary environment or config values through sandbox factory exec unless a later story defines a redaction-safe contract for them.
---
## 2026-06-21 07:41 CST - US-008
- Added a sandbox timeline writer that tees remote provider exec output to the caller, splits complete output lines, sanitizes persisted payloads with the sandbox redactor, and records each line as a `command_output_summary` timeline event.
- Tagged remote output events with `source=remote_sandbox`, `stream=remote`, sandbox name, and provider metadata so factory status consumers can distinguish sandbox execution logs from local pipeline progress.
- Added a fake-dependency test that writes split remote output containing a sandbox IP address and verifies the durable timeline events are line-based, tagged, and redacted before persistence.
- Files changed: `cmd/factory_sandbox_executor.go`, `cmd/factory_sandbox_executor_test.go`, `.hal/prd.json`, `.hal/progress.txt`, `AGENTS.md`.
- Quality checks: focused `go test ./cmd -run 'TestRunFactorySandboxExecutorWithDeps|TestFactorySandboxRemoteAutoArgs'`, `make test`, and `make vet` passed.
- **Learnings for future iterations:**
  - Capture remote sandbox output at the executor writer boundary so provider exec implementations remain fakeable and user-facing output still streams normally.
  - Flush partial remote output before appending terminal executor timeline events so event ordering and sequence numbers stay coherent.
  - The sandbox executor can sanitize known sandbox addresses via `sandboxRedactor(false, nil, target)` without passing arbitrary env/config values into remote execution.
---
## 2026-06-21 07:46 CST - US-009
- Added explicit remote sandbox execution start and completion timeline events around provider execution.
- Preserved existing factory success conventions by letting the outer factory runner record artifacts, mark sandbox-backed runs `succeeded`, set `currentStep=done`, and append the terminal pipeline completion event after remote execution succeeds.
- Added a full sandbox success-path test verifying persisted sandbox name, provider, running status, safe connection metadata, SSH/cleanup handoff commands, artifacts, remote lifecycle/output timeline events, and final success summary.
- Files changed: `cmd/factory_sandbox_executor.go`, `cmd/factory_sandbox_executor_test.go`, `cmd/factory_test.go`, `.hal/prd.json`, `.hal/progress.txt`.
- Quality checks: focused `go test ./cmd -run 'TestRunFactorySandboxExecutorWithDeps|TestRunFactoryRunWithDepsPersistsSuccessfulSandboxRunOutcome|TestRunFactoryRunWithDepsPersistsSuccessfulStatusAndResult'`, `make test`, and `make vet` passed.
- **Learnings for future iterations:**
  - Keep remote sandbox start/output/completion timeline writes behind `factorySandboxTimelineWriter` so sequence numbers stay coherent across injected append-event fakes and durable stores.
  - Sandbox executor success should record remote lifecycle details, but the outer `runFactoryRunWithDeps` success path should remain responsible for artifacts, final `RunStatusSucceeded`, `FinishedAt`, and local result rendering.
  - Sandbox remote output is intentionally streamed to the user-facing writer in human mode; tests that assert final JSON should avoid mixing synthetic remote log bytes into the JSON buffer unless that contract is changed separately.
---
