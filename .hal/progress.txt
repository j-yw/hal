## Codebase Patterns
- Config env var constants live in `internal/cloud/deploy/config.go` — add new env constants to the same const block.
- `LoadConfig(getenv)` pattern enables testable config loading; tests use inline `func(key string) string { return env[key] }` closures.
- `ValidateStore()` is DB-only validation; `Validate()` is full validation. Both delegate to `validateAdapter()` for DB checks.
- URL fallback precedence is implemented directly in `LoadConfig` (check primary, fallback to legacy alias).
- Config fields are value types (not pointers) since these come from env vars, not YAML.
- SDK client constructor pattern: `SDKClientConfig` struct → pre-validate required fields → delegate to `daytona.NewClientWithConfig` → wrap SDK errors with `%w`.
- SDK client tests are in `sdk_client_test.go` (package-internal); HTTP client tests remain in `runner_test.go`.
- The `daytona.NewClientWithConfig` constructor does not make network calls — it only configures HTTP clients, so tests can construct real SDK clients without mocking.
- SDK sandbox lifecycle: `client.Create` → `*daytona.Sandbox`, `client.Get(id)` → `*daytona.Sandbox`, then `sandbox.Delete(ctx)` — the delete is on the sandbox object, not the client.
- Use cancelled-context tests (`ctx, cancel := ...; cancel()`) to exercise SDK error wrapping without a mock or live server.
- SDK `sandbox.Process.ExecuteCommand(ctx, cmd, opts...)` returns `*types.ExecuteResponse{ExitCode, Result, Artifacts}` — map `Result` to stdout, prefer `Artifacts.Stdout` when available.
- SDK options are functional: `options.WithCwd(dir)`, `options.WithExecuteTimeout(dur)` — build a `[]func(*options.ExecuteCommand)` slice and spread with `opts...`.
- SDK `ProcessService.GetSessionCommandLogs(ctx, sessionID, commandID)` returns `map[string]any` — access logs via `logsMap["logs"].(string)` with zero-value fallback.
- SDK `client.List(ctx, labels, page *int, limit *int)` returns `*PaginatedSandboxes` — use for health probes with page=1, limit=1 and `daytona.Version` for version info.

## 2026-02-12 - US-001
- Removed `HAL_CLOUD_RUNNER_URL` and `HAL_CLOUD_RUNNER_SERVICE_TOKEN` constants, fields, and env parsing from `internal/cloud/deploy/config.go`
- Added `DAYTONA_API_KEY` (required), `DAYTONA_API_URL` (optional), `DAYTONA_SERVER_URL` (legacy alias fallback), and `DAYTONA_TARGET` (optional)
- `LoadConfig` prefers `DAYTONA_API_URL` and falls back to `DAYTONA_SERVER_URL` only when `DAYTONA_API_URL` is unset
- `Validate()` now requires non-empty `DaytonaAPIKey` instead of `RunnerURL`/`RunnerServiceToken`
- `ValidateStore()` remains DB-only (unchanged behavior)
- Files changed: `internal/cloud/deploy/config.go`, `internal/cloud/deploy/config_test.go`
- **Learnings for future iterations:**
  - No other Go files in the project reference `deploy.Config` fields directly — the config struct is only used within the deploy package tests and config loading, making field changes safe.
  - The smoke.go file references `runnerURL` as a parameter, not as a Config field — it will need separate attention in US-008.
  - Runner-related env vars also appear in `deploy/docker-compose.yml` and `deploy/.env.example` — those are addressed in US-009.
---

## 2026-02-12 - US-002
- Added `github.com/daytonaio/daytona/libs/sdk-go v0.141.0` to go.mod (with transitive deps: api-client-go, toolbox-api-client-go)
- Created `internal/cloud/runner/sdk_client.go` with `SDKClientConfig` struct and `NewSDKClient` constructor
- Constructor validates APIKey is required, then delegates to `daytona.NewClientWithConfig` with `types.DaytonaConfig{APIKey, APIUrl, Target}`
- Constructor wraps SDK init failures with `fmt.Errorf("sdk runner client: init: %w", err)`
- Created `internal/cloud/runner/sdk_client_test.go` with table-driven constructor tests covering empty APIKey, valid configs, error prefix, and wrapping behavior
- Files changed: `go.mod`, `go.sum`, `internal/cloud/runner/sdk_client.go`, `internal/cloud/runner/sdk_client_test.go`
- **Learnings for future iterations:**
  - The Daytona Go SDK brings significant transitive dependencies (AWS SDK, OpenTelemetry, gRPC, websocket) — expect a large go.sum diff.
  - `NewClientWithConfig` is a pure config constructor (no network calls), so tests can create real SDK clients safely.
  - The `SDKClient` struct stores `*daytona.Client` — future stories (US-003+) will add Runner interface methods that delegate to this client.
  - The SDK `types.DaytonaConfig` also supports `JWTToken` and `OrganizationID` — we only expose APIKey/APIURL/Target since that's what our deploy config provides.
---

## 2026-02-12 - US-003
- Added `CreateSandbox` method to `SDKClient`: validates nil request and empty image, then calls `client.Create(ctx, types.ImageParams{Image, SandboxBaseParams{EnvVars}})`, maps SDK `Sandbox` to `runner.Sandbox` (ID, State→Status, time.Now()→CreatedAt)
- Added `DestroySandbox` method to `SDKClient`: validates empty sandboxID, retrieves sandbox via `client.Get(ctx, sandboxID)`, then calls `sandbox.Delete(ctx)`
- Both methods wrap failures with `%w` and operation-specific prefixes (`sdk runner client: create sandbox:`, `sdk runner client: destroy:`)
- Added tests: validation paths (nil request, empty image, empty ID), error prefix checks, and SDK failure paths (cancelled context forces SDK-level errors to exercise wrapping)
- Files changed: `internal/cloud/runner/sdk_client.go`, `internal/cloud/runner/sdk_client_test.go`
- **Learnings for future iterations:**
  - SDK `client.Create` returns `*daytona.Sandbox` with `ID`, `Name`, `State` (typed `apiclient.SandboxState`), and service fields (FileSystem, Git, Process, etc.) — cast `State` to string for `runner.Sandbox.Status`.
  - SDK `client.Create` accepts `any` params (switch on `types.ImageParams` or `types.SnapshotParams`) — we use `ImageParams` since our `CreateSandboxRequest.Image` maps to it.
  - SDK `Sandbox.Delete(ctx)` delegates to `DeleteWithTimeout(ctx, 60s)` — no separate timeout needed at our layer.
  - Cancelled-context tests are the cleanest way to exercise SDK error wrapping without a mock or a live server — `ctx, cancel := context.WithCancel(ctx); cancel()` triggers immediate SDK failure.
  - `DestroySandbox` uses a two-step get-then-delete pattern because the SDK's `Delete` is on the `*Sandbox` object, not on the client.
---

## 2026-02-12 - US-004
- Added `Exec` method to `SDKClient`: validates nil request, empty sandboxID, and empty command, then retrieves sandbox via `client.Get`, builds functional options slice for WorkDir/Timeout, calls `sandbox.Process.ExecuteCommand(ctx, cmd, opts...)`
- Maps `ExecuteResponse` to `runner.ExecResult`: `ExitCode` direct, `Result` to `Stdout`, prefers `Artifacts.Stdout` when available
- Options applied conditionally: `options.WithCwd` when `req.WorkDir != ""`, `options.WithExecuteTimeout` when `req.Timeout > 0`
- All failures wrapped with `%w` and `sdk runner client: exec:` prefix
- Added tests: validation table (nil request, empty sandboxID, empty command, empty command with workdir), SDK failure via cancelled context, SDK failure with options
- Files changed: `internal/cloud/runner/sdk_client.go`, `internal/cloud/runner/sdk_client_test.go`
- **Learnings for future iterations:**
  - SDK `ExecuteCommand` uses variadic functional options pattern: `func(*options.ExecuteCommand)` — build a slice and spread it
  - `ExecuteResponse.Result` is the combined output; `Artifacts.Stdout` is structured stdout when available (Artifacts can be nil)
  - The SDK converts `time.Duration` timeout to `int32` seconds internally, so pass the `time.Duration` directly to `WithExecuteTimeout`
  - Exec follows the same get-then-operate pattern as DestroySandbox: `client.Get` first, then operate on the returned `*Sandbox` object's services
---

## 2026-02-12 - US-005
- Added `StreamLogs` method to `SDKClient`: validates empty sandboxID, retrieves sandbox via `client.Get`, calls `sandbox.Process.GetSessionCommandLogs(ctx, "default", "default")`, returns `io.NopCloser(strings.NewReader(logs))`
- `GetSessionCommandLogs` returns `map[string]any` with a `"logs"` key containing the log text — type-assert with zero-value fallback
- All failures wrapped with `%w` and `sdk runner client: stream logs:` prefix
- Added tests: validation table (empty sandboxID), SDK failure via cancelled context
- Files changed: `internal/cloud/runner/sdk_client.go`, `internal/cloud/runner/sdk_client_test.go`
- **Learnings for future iterations:**
  - SDK `ProcessService.GetSessionCommandLogs(ctx, sessionID, commandID)` returns `map[string]any` not a string — access logs via `logsMap["logs"].(string)`
  - The "default" session and "default" command IDs are used for standard log retrieval
  - StreamLogs follows the same get-then-operate pattern as DestroySandbox and Exec
  - The `io.NopCloser(strings.NewReader(...))` pattern fulfills the `io.ReadCloser` return type without any actual streaming or cleanup needed
---

## 2026-02-12 - US-006
- Added `Health` method to `SDKClient`: probes Daytona reachability via `client.List(ctx, nil, &page1, &limit1)`, returns `HealthStatus{OK:true, Version:daytona.Version}` on success
- Pagination params are `*int` pointers (page=1, limit=1) — minimal probe to verify API reachability without fetching real data
- Failures wrapped with `%w` and `sdk runner client: health:` prefix
- Added tests: SDK failure via cancelled context, version/interface compliance verification
- Files changed: `internal/cloud/runner/sdk_client.go`, `internal/cloud/runner/sdk_client_test.go`
- **Learnings for future iterations:**
  - SDK `client.List` takes `(ctx, labels map[string]string, page *int, limit *int)` and returns `*PaginatedSandboxes` — use `nil` labels and pointer-to-int for pagination
  - `daytona.Version` is a package-level `var` (not const) embedded at SDK build time — may be empty in test builds without the embedded VERSION file
  - Health has no validation step (no input params) so the only test paths are SDK success/failure — use cancelled-context for failure and interface compliance check for success wiring
---

## 2026-02-12 - US-007
- Deleted `internal/cloud/runner/client.go` (HTTP runner implementation: ClientConfig, Client, NewClient, all HTTP methods, helpers)
- Deleted `internal/cloud/runner/runner_test.go` (HTTP-focused tests: constructor, header, CRUD, context cancellation, interface compliance)
- Updated `internal/cloud/runner/runner.go` package comment to remove "HTTP client implementation" mention
- Verified no external references to HTTP runner types (`runner.Client`, `runner.NewClient`, `runner.ClientConfig`) exist in the codebase
- `go test ./internal/cloud/runner/...` passes with SDK-only implementation (sdk_client.go + sdk_client_test.go)
- `make test`, `go vet ./...`, `go build ./...` all pass
- Files changed: `internal/cloud/runner/client.go` (deleted), `internal/cloud/runner/runner_test.go` (deleted), `internal/cloud/runner/runner.go` (comment update)
- **Learnings for future iterations:**
  - The HTTP runner types (`Client`, `ClientConfig`, `NewClient`) were only used within the runner package — no external Go files import or reference them, making deletion safe
  - The `runner_test.go` file was entirely HTTP-focused (used `httptest.NewServer`, `net/http`, etc.) — SDK tests live separately in `sdk_client_test.go`
  - After deletion, the runner package contains only: `runner.go` (interface + types), `sdk_client.go` (SDK implementation), `sdk_client_test.go` (SDK tests)
  - The compile-time interface check `var _ Runner = (*Client)(nil)` was in `client.go` — SDK compliance is verified in `sdk_client_test.go` via `TestSDKClientHealthReturnsVersion` which constructs and uses the client
---

## 2026-02-12 - US-008
- Updated `RunSmoke` in `internal/cloud/deploy/smoke.go` to treat empty `runnerURL` as SDK-direct mode, returning a synthetic healthy `SmokeResult{Service:"runner", URL:"sdk-direct", OK:true}`
- Non-empty runner URL behavior is unchanged — still performs HTTP health check
- Added `TestRunSmoke_EmptyRunnerURL_SyntheticHealth` test verifying synthetic healthy result with OK=true, URL="sdk-direct", StatusCode=0, no error
- Added `TestRunSmoke_NonEmptyRunnerURL_HTTPCheck` test verifying HTTP check still occurs for non-empty URLs
- Files changed: `internal/cloud/deploy/smoke.go`, `internal/cloud/deploy/smoke_test.go`
- **Learnings for future iterations:**
  - `RunSmoke` takes `runnerURL` as a plain string parameter, not from `Config` — callers pass whatever URL they have, so the empty-string check is the right place to handle SDK-direct mode
  - Synthetic results use `StatusCode: 0` (zero value) since no HTTP call was made — tests should assert this explicitly
  - The `URL` field on synthetic results is set to `"sdk-direct"` as a human-readable marker that distinguishes it from HTTP-checked results in reports
---

## 2026-02-12 - US-009
- Removed `daytona-runner` service from `deploy/docker-compose.yml` (build, ports, environment, healthcheck)
- Removed `depends_on: daytona-runner` from `control-plane` service
- Removed `HAL_CLOUD_RUNNER_URL` and `HAL_CLOUD_RUNNER_SERVICE_TOKEN` env vars from both `control-plane` and `worker` services
- Added `DAYTONA_API_KEY`, `DAYTONA_API_URL`, and `DAYTONA_TARGET` env vars to `worker` service
- Updated `deploy/.env.example`: removed `HAL_CLOUD_RUNNER_*` entries, added documented `DAYTONA_API_KEY` (required), `DAYTONA_API_URL` (optional), `DAYTONA_SERVER_URL` (legacy alias, commented), and `DAYTONA_TARGET` (optional, commented)
- Updated compose header comment to remove daytona-runner mention and smoke check runner URL
- Verified `docker compose -f deploy/docker-compose.yml config` succeeds
- Files changed: `deploy/docker-compose.yml`, `deploy/.env.example`
- **Learnings for future iterations:**
  - The `daytona-runner` service was only referenced in compose as a service definition and `depends_on` — no other deploy files reference it by service name
  - `control-plane` no longer needs runner env vars since it doesn't proxy runner requests — the worker connects directly via SDK
  - `DAYTONA_API_KEY` in compose uses `${DAYTONA_API_KEY}` (no default) to enforce it's required, while `DAYTONA_API_URL` and `DAYTONA_TARGET` use `${VAR:-}` for optional with empty default
  - `.env.example` documents the `DAYTONA_SERVER_URL` legacy alias as a comment to show it exists without encouraging its use
---
