# Ralph Progress Log

## Codebase Patterns

- Factory policy defaults live in `internal/factory.FactoryPolicy`; use `DefaultFactoryPolicy` and `Validate`, where zero attempt limits mean no policy cap, enum values normalize to lowercase, and validation errors use `factory.policy.<field>` paths.
- Factory policy config loading lives in `internal/factory.LoadPolicyConfig`; merge pointer-backed `factory.policy` YAML fields over `DefaultFactoryPolicy` so explicit false, zero, and empty values are preserved without coupling direct `hal auto` config loading to factory policy validation.
- Policy decision timeline events use `factory.EventTypePolicyDecision` plus `factory.PolicyDecisionMetadata`; append them through `recordFactoryPolicyDecision` so sequence assignment stays with existing factory timeline helpers and metadata remains limited to `policyField`, `decision`, `outcome`, and `reason`.
- Factory run creation policy gates live in `cmd/factory.go` after the pending run record and run-created event are persisted but before `executeFactoryRun`; use injected policy/engine dependencies, record rejections with `recordFactoryPolicyDecision`, and fail at the `policy` step without invoking pipeline or sandbox execution.

---

## 2026-06-21 09:01 CST - US-001
- Implemented durable factory policy defaults and validation for sandbox requirement, allowed engines, attempt limits, verification gating, PR creation, merge permission, and cleanup behavior.
- Added policy constants/helpers plus focused tests for conservative defaults, invalid negative attempt limits, unsupported engine identifiers, invalid cleanup behavior, and enum normalization.
- Files changed: `internal/factory/policy.go`, `internal/factory/policy_test.go`, `internal/factory/types_test.go`, `.hal/prd.json`, `.hal/progress.txt`, `AGENTS.md`.
- Quality checks: `go test -timeout 60s ./internal/factory`, `go test -timeout 300s ./...`, `make vet`, `make test`; `make lint` skipped because `golangci-lint` is not installed.
- **Learnings for future iterations:**
  - Keep durable factory DTOs in `internal/factory` and add them to `TestFactoryTypesHaveJSONTags` when they expose machine-readable fields.
  - Policy attempt limits use `0` as unlimited/no cap so existing factory behavior remains unchanged until a stricter policy is configured.
  - Field-specific policy validation errors should use `factory.policy.<field>` paths for future config-loading and command-surface diagnostics.
---

## 2026-06-21 09:08 CST - US-002
- Implemented factory policy config loading from `.hal/config.yaml` via `factory.LoadPolicyConfig`, merging configured `factory.policy` values over `DefaultFactoryPolicy`.
- Added policy config tests for missing config/default loading, partial field default merging, strict explicit policy values, explicit zero/empty values, and invalid configured values with `factory.policy` validation paths.
- Ran `make fmt`; it also normalized existing gofmt alignment/indentation in `internal/ci/auth.go`, `internal/ci/auth_test.go`, and `internal/ci/push.go`.
- Files changed: `internal/factory/policy_config.go`, `internal/factory/policy_test.go`, `internal/ci/auth.go`, `internal/ci/auth_test.go`, `internal/ci/push.go`, `.hal/prd.json`, `.hal/progress.txt`, `AGENTS.md`.
- Quality checks: `go test -timeout 60s ./internal/factory`, `make fmt`, `make vet`, `make test`, `make lint` (advisory: `golangci-lint` is not installed; Makefile exited 0).
- **Learnings for future iterations:**
  - Keep factory-only policy configuration in `internal/factory` so direct `hal auto` config loading remains unaffected until factory command code explicitly opts into policy enforcement.
  - Use pointer-backed raw YAML fields for policy config booleans, integers, strings, and slices so explicit `false`, `0`, `""`, and `[]` can be distinguished from omitted fields.
  - Copy configured slices with `make`/`copy` rather than `append([]T(nil), values...)` when tests need to preserve an explicit empty slice instead of collapsing it to nil.
---

## 2026-06-21 09:15 CST - US-003
- Implemented structured factory policy decision timeline support with a new `policy_decision` event type, decision/outcome constants for allowed execution, rejected execution, passed gates, and blocked gates, and a safe `PolicyDecisionMetadata` shape.
- Added command-layer recording through the existing factory timeline append helper so policy decision events get deterministic sequence numbers alongside other run events.
- Added tests for policy decision event type support, metadata JSON field shape, safe metadata keys, and one allowed plus one blocked policy decision timeline event.
- Files changed: `internal/factory/types.go`, `internal/factory/types_test.go`, `internal/factory/store_test.go`, `cmd/factory.go`, `cmd/factory_test.go`, `.hal/prd.json`, `.hal/progress.txt`, `AGENTS.md`.
- Quality checks: `go test -timeout 60s ./internal/factory`, `go test -timeout 120s ./cmd`, `make vet`, `make test`, `make lint` (advisory: `golangci-lint` is not installed; Makefile exited 0).
- **Learnings for future iterations:**
  - Policy decision events should be appended through `recordFactoryPolicyDecision` rather than hand-building timeline metadata so all policy gates share the same event type, sequence behavior, and safe metadata keys.
  - Durable policy decision fields belong in `internal/factory` with JSON tag coverage in `internal/factory/types_test.go`; command tests should assert append order and exact metadata keys for timeline events.
---

## 2026-06-21 09:23 CST - US-004
- Implemented factory run creation policy enforcement for `factory.policy.sandboxRequired` and `factory.policy.allowedEngines`, resolving the factory engine through an injected dependency before execution starts.
- Rejected runs now persist as failed at the `policy` step, record a `rejected_execution` policy decision, and add failure classification without starting the local pipeline or sandbox executor.
- Added focused command tests for strict sandbox-required rejection and disallowed engine rejection before sandbox execution; existing default local execution coverage verifies the allowed default path remains unchanged.
- Files changed: `cmd/factory.go`, `cmd/factory_test.go`, `.hal/prd.json`, `.hal/progress.txt`, `AGENTS.md`.
- Quality checks: `go test -timeout 180s ./cmd`, `go test -timeout 60s ./internal/factory`, `go test -timeout 300s ./...`, `make vet`, `make test`, `make lint` (advisory: `golangci-lint` is not installed; Makefile exited 0).
- **Learnings for future iterations:**
  - Factory run creation gates should execute after the pending run record exists but before `executeFactoryRun`, so rejected runs are inspectable and never call engine, GitHub, or sandbox executor dependencies.
  - Use injected `loadPolicy` and `loadEngine` dependencies in command tests to keep policy enforcement deterministic and isolated from local config.
---
