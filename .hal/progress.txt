# Ralph Progress Log

## Codebase Patterns

- Factory run/event schemas live in `internal/factory`; exported JSON DTO fields should use explicit camelCase tags and be locked with marshal/unmarshal key tests before storage or CLI layers consume them.
- Factory store paths live in `internal/factory`; the default root is `sandbox.GlobalDir()/factory` with `runs/` and `timelines/` directories created at `0700`, and read-only list operations treat missing directories as empty state.
- Factory run records are persisted through `Store.SaveRun`/`Store.LoadRun` as `runs/<runID>.json`; writes use `0600` temp files plus rename, and missing loads must wrap `fs.ErrNotExist`.
- Use `Store.ListRuns` for full factory run records ordered newest-first by latest created/updated timestamp with run ID as the stable tie-breaker; keep `ListRunIDs` for lexicographic ID-only listings.
- Factory timelines are persisted through `Store.AppendEvent`/`Store.LoadEvents` as `timelines/<runID>.json`; append rewrites the JSON array through the same `0600` temp-file-plus-rename path and `LoadEvents` returns empty state for missing timelines.
- Factory read/list paths should only consider committed `*.json` files via `isCommittedStoreFile`; leftover `*.tmp` and `*.bak` artifacts must stay invisible to run and timeline reads.
- Factory command surfaces live in `cmd/factory.go`; `hal factory list --json` should emit `factory-list-v1` with `runs` summaries from `Store.ListRuns`, omit full artifacts/timelines, and regenerate `docs/cli/hal_factory*.md` with `make docs-cli`.
- `hal factory status <run-id> --json` should emit `factory-status-v1` with a full `run` and append-ordered `timeline`; missing run IDs must return before writing any JSON payload.
- Factory contract docs are split by JSON surface: `factory-list-v1.md`, `factory-status-v1.md`, and `factory-timeline-v1.md`; example payloads live in `docs/contracts/examples/` and should mirror `cmd/factory.go` response DTOs plus `internal/factory` JSON tags.

---

## 2026-06-20 23:24 CST - US-001
- Implemented centralized factory run and event schemas with stable JSON field names, run status constants, and supported timeline event type constants.
- Added JSON contract-style tests for run records, source metadata, artifact references, failure summaries, events, optional field omission, constants, and round-trip marshal/unmarshal behavior.
- Files changed: `internal/factory/types.go`, `internal/factory/types_test.go`, `.hal/prd.json`, `.hal/progress.txt`
- Quality checks: `go test -timeout=120s ./...` passed; `perl -e 'alarm shift; exec @ARGV' 120 go vet ./...` passed; `golangci-lint` not installed, so lint was skipped.
- **Learnings for future iterations:**
  - Existing machine-readable DTO tests in `internal/ci` and `internal/sandbox` use explicit JSON tags plus map-based key assertions; factory schemas now follow the same pattern.
  - Keep optional JSON fields behind `omitempty` and test both populated and omitted forms so later contract docs can rely on the behavior.
  - `EventRecord` uses a monotonically increasing `sequence` field rather than a separate event ID, which gives later append/load work a clear ordering key.
---

## 2026-06-20 23:31 CST - US-002
- Implemented factory store path resolution under Hal's global config directory with `Store`, `DefaultStore`, `StoreDir`, `EnsureStoreDir`, `RunsDir`, `TimelinesDir`, and deterministic empty-state `ListRunIDs`.
- Added tests for `HAL_CONFIG_HOME`/`XDG_CONFIG_HOME`/`HOME` precedence, restrictive `0700` store directory creation, idempotent ensure behavior, missing-store list behavior, and deterministic run ID ordering.
- Files changed: `internal/factory/store.go`, `internal/factory/store_test.go`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`
- Quality checks: `go test -timeout=120s ./internal/factory` passed; `go test -timeout=120s ./...` passed; `perl -e 'alarm shift; exec @ARGV' 180 go vet ./...` passed; `golangci-lint` not installed, so lint was skipped.
- **Learnings for future iterations:**
  - Use `sandbox.GlobalDir()` for the factory default store root so factory state follows the same global config precedence as sandbox state while staying outside per-project `.hal/`.
  - `EnsureStoreDir` should create the global root, `factory/`, `factory/runs/`, and `factory/timelines/` with restrictive `0700` permissions.
  - Read-only factory listing paths should return empty results for missing directories instead of creating global state.
---

## 2026-06-20 23:36 CST - US-003
- Implemented durable factory run record persistence with `Store.SaveRun` and `Store.LoadRun`, including stable run ID pathing, atomic temp-file-plus-rename writes, restrictive file permissions, update support, and missing-run `fs.ErrNotExist` compatibility.
- Added focused store tests for save/load across a new store instance, existing record updates, missing-run errors, required run IDs, committed file permissions, and temp-file cleanup.
- Files changed: `internal/factory/store.go`, `internal/factory/store_test.go`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`
- Quality checks: `go test ./internal/factory` passed; `go test ./...` passed; `go vet ./...` passed; `golangci-lint` not installed, so lint was skipped.
- **Learnings for future iterations:**
  - Use `Store.SaveRun` and `Store.LoadRun` as the factory run-record API instead of reading or writing files directly.
  - Factory run record writes follow the sandbox registry durability pattern: marshal indented JSON, write `<run>.json.tmp` at `0600`, then rename to the committed `.json` file.
  - Missing factory run loads intentionally wrap `fs.ErrNotExist` so callers and command layers can use `errors.Is(err, fs.ErrNotExist)`.
---

## 2026-06-20 23:42 CST - US-004
- Implemented `Store.ListRuns` to load committed factory run records and return them newest-first by latest created/updated timestamp with run ID as a deterministic tie-breaker.
- Added focused tests for missing-store empty-state behavior, no directory creation on read-only listing, ignored non-record artifacts, full record loading, newest-first ordering, and stable tie-breaking.
- Files changed: `internal/factory/store.go`, `internal/factory/store_test.go`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`
- Quality checks: `go test ./internal/factory` passed; `go test ./...` passed; `go vet ./...` passed; `golangci-lint` not installed, so lint was skipped.
- **Learnings for future iterations:**
  - `Store.ListRuns` is the full-record API for later factory list/status commands; `Store.ListRunIDs` remains an ID-only lexicographic helper.
  - Full run listing should only read committed `*.json` files under `factory/runs/` and should ignore directories, non-JSON files, and temp-like files such as `*.json.tmp`.
  - Read-only factory listing methods should not call `Ensure`, so missing store directories remain empty state without creating global config artifacts.
---

## 2026-06-20 23:48 CST - US-005
- Implemented durable factory timeline appends with `Store.AppendEvent` and read-back with `Store.LoadEvents`, preserving append order across store instances.
- Added focused tests for append/load persistence, committed timeline file permissions, temp-file cleanup, missing-timeline empty state, supported event types, and run ID validation.
- Files changed: `internal/factory/store.go`, `internal/factory/store_test.go`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`
- Quality checks: `go test ./internal/factory` passed; `go test ./...` passed; `go vet ./...` passed; `golangci-lint` not installed, so lint was skipped.
- **Learnings for future iterations:**
  - Use `Store.AppendEvent` and `Store.LoadEvents` as the factory timeline API instead of reading or writing timeline files directly.
  - Factory timeline files live at `factory/timelines/<runID>.json` as ordered JSON arrays and use the same `0600` temp-file-plus-rename durability pattern as run records.
  - Missing timelines intentionally load as empty event slices so future status/list command layers can display runs that have no events yet.
---

## 2026-06-20 23:53 CST - US-006
- Implemented explicit committed-file filtering for factory run list/read paths so stale temp and backup artifacts are ignored.
- Added regression tests proving leftover `*.json.tmp` run and timeline files do not replace or corrupt previously committed records.
- Files changed: `internal/factory/store.go`, `internal/factory/store_test.go`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`
- Quality checks: `go test -timeout=120s ./internal/factory` passed; `go test -timeout=120s ./...` passed; `perl -e 'alarm shift; exec @ARGV' 180 go vet ./...` passed; `golangci-lint` not installed, so lint was skipped.
- **Learnings for future iterations:**
  - Use `isCommittedStoreFile` when adding factory read/list paths that scan storage directories.
  - `LoadRun` and `LoadEvents` should keep reading only committed paths; temp files may contain conflicting or partial JSON and should never be consulted.
  - Temp-artifact regression tests should assert both direct loads and list surfaces so future command layers inherit the same safety.
---

## 2026-06-21 00:00 CST - US-007
- Implemented the `hal factory list` command family with `--json` output using the `factory-list-v1` contract and compact run summaries sourced from `Store.ListRuns`.
- Added focused command tests for empty-state JSON, newest-first ordering, top-level/run JSON keys, omitted artifact/timeline payloads, and command registration with the `--json` flag.
- Regenerated CLI docs so the new `hal factory` and `hal factory list` pages are included in `docs/cli`.
- Files changed: `cmd/factory.go`, `cmd/factory_test.go`, `cmd/docs_metadata_core_test.go`, `cmd/docs_metadata_archive_sandbox_test.go`, `docs/cli/hal.md`, `docs/cli/hal_factory.md`, `docs/cli/hal_factory_list.md`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`
- Quality checks: `go test -timeout=120s ./cmd` passed; `go test -timeout=120s ./...` passed; `perl -e 'alarm shift; exec @ARGV' 180 go vet ./...` passed; `perl -e 'alarm shift; exec @ARGV' 180 make docs-check` passed; `golangci-lint` not installed, so lint was skipped.
- **Learnings for future iterations:**
  - New top-level Cobra command families need metadata coverage and regenerated `docs/cli` artifacts because `make docs-check` is part of CI.
  - Keep factory list JSON summary-only: expose `artifactCount` but omit full `artifacts`, `events`, and timeline fields so status/detail commands can own full run inspection later.
  - Command tests can use isolated `factory.NewStore(t.TempDir())` roots through injected deps instead of mutating global factory config paths.
---

## 2026-06-21 00:06 CST - US-008
- Implemented `hal factory status <run-id> --json` with the `factory-status-v1` contract, including the full factory run record and append-ordered timeline events.
- Added missing-run handling that returns a non-zero error before writing JSON, plus command tests for JSON shape, timeline order, command registration, and no-payload missing-run behavior.
- Regenerated factory CLI docs for the new `hal factory status` page.
- Files changed: `cmd/factory.go`, `cmd/factory_test.go`, `docs/cli/hal_factory.md`, `docs/cli/hal_factory_status.md`, `AGENTS.md`, `.hal/prd.json`, `.hal/progress.txt`
- Quality checks: `go test -timeout=120s ./cmd` passed; `go test -timeout=120s ./...` passed; `perl -e 'alarm shift; exec @ARGV' 180 go vet ./...` passed; `perl -e 'alarm shift; exec @ARGV' 180 make docs-check` passed; `golangci-lint` not installed, so lint was skipped.
- **Learnings for future iterations:**
  - Keep one-run factory inspection in `hal factory status`; `hal factory list` remains summary-only.
  - Load the run before loading the timeline so missing IDs can fail without emitting partial or malformed JSON.
  - Factory command JSON tests should assert top-level keys and timeline append order rather than sorting by event sequence.
---

## 2026-06-21 00:12 CST - US-009
- Documented the `factory-list-v1`, `factory-status-v1`, and factory timeline event JSON surfaces under `docs/contracts/`.
- Added example JSON payloads for factory list output and factory status output with timeline events.
- Covered required fields, optional fields, status values, event types, ordering rules, missing-run behavior, empty-state behavior, and the unchanged `.hal/prd.json`, `.hal/auto-state.json`, and `.hal/progress.txt` contracts.
- Files changed: `docs/contracts/factory-list-v1.md`, `docs/contracts/factory-status-v1.md`, `docs/contracts/factory-timeline-v1.md`, `docs/contracts/examples/factory-list-v1.json`, `docs/contracts/examples/factory-status-v1.json`, `.hal/prd.json`, `.hal/progress.txt`
- Quality checks: `jq empty docs/contracts/examples/factory-list-v1.json docs/contracts/examples/factory-status-v1.json` passed; `go test -timeout=120s ./...` passed; `go vet ./...` passed; `make docs-check` passed; `golangci-lint` not installed, so lint was skipped.
- **Learnings for future iterations:**
  - Treat the timeline contract as the event surface embedded in `factory-status-v1` unless a standalone timeline command is added.
  - Keep factory contract examples aligned with `cmd/factory.go` response structs and `internal/factory` JSON tags, including `omitempty` behavior.
  - Contract docs should explicitly call out no-payload error cases, because command handlers intentionally fail before rendering JSON for missing run IDs.
---
